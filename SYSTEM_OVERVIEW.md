# شرح تقسيم النظام (عام)

هذا الملف يشرح تقسيم المشروع بشكل عام: كل جزء أين موجود وما فائدته.

## نظرة عامة على المجلدات الرئيسية

- `frontend/`
  واجهة المستخدم (React + Vite). هنا توجد الصفحات والتصميم وتجربة المستخدم.
- `backend/`
  السيرفر والـ API (Node.js + Express + TypeScript). هنا منطق الأعمال والتعامل مع قاعدة البيانات.
- `DB/`
  نسخة قاعدة البيانات بصيغة SQL جاهزة للاستيراد.

## 1) `frontend/` — الواجهة

**الفائدة:** عرض النظام للمستخدم، تنفيذ الشاشات، إرسال الطلبات للـ API، واستقبال النتائج.

### أهم التقسيمات

- `src/pages/`
  كل صفحة موجودة داخل مجلد خاص بها. كل مجلد صفحة يحتوي غالبًا:
  - ملف `.tsx` للواجهة والمنطق.
  - ملف `module.css` للتصميم الخاص بهذه الصفحة فقط.

  أمثلة:
  - `employees/EmployeeList/` عرض الموظفين
  - `employees/AddEmployee/` إضافة موظف
  - `employees/EditEmployee/` تعديل موظف
  - `employees/EmployeeDetails/` تفاصيل موظف
  - `dashboard/` لوحة التحكم
  - `reports/` التقارير
  - `salary-slips/` كشوف الرواتب
  - `profile/` الملف الشخصي والإعدادات

- `src/components/`
  مكونات مشتركة مثل الهيدر والسايدبار والأزرار والعناصر المعاد استخدامها.

- `src/services/api/`
  ملفات الاتصال بالباك‑إند (الـ API). هنا يتم إرسال:
  - `GET` للعرض
  - `POST` للإضافة
  - `PUT/PATCH` للتعديل
  - `DELETE` للحذف

- `public/`
  ملفات ثابتة (صور، أيقونات) تُخدم مباشرةً.

## 2) `backend/` — السيرفر

**الفائدة:** تنفيذ منطق النظام، التحقق من البيانات، التعامل مع قواعد البيانات، وإرجاع النتائج للواجهة.

### أهم التقسيمات داخل `src/`

- `infrastructure/http/express/`
  إعداد السيرفر والمسارات:
  - `app.ts` تهيئة الـ Express (middlewares, CORS, routes)
  - `routes/` جميع مسارات الـ API (Endpoints)

  مثال على المسارات:
  - `employees.routes.ts` (CRUD للموظفين)
  - `allowances.routes.ts` (CRUD للعلاوات)
  - `deductions.routes.ts` (CRUD للخصومات)
  - `bonuses.routes.ts` (CRUD للمكافآت)
  - `salarySlips.routes.ts` (CRUD لكشوف الرواتب)
  - `reports.routes.ts` (التقارير)
  - `auth.routes.ts` (تسجيل الدخول والصلاحيات)

- `infrastructure/database/repositories/`
  طبقة الوصول للبيانات (Database Access).
  كل ملف يمثل جدولًا ويحتوي عمليات:
  - إضافة (Insert)
  - تعديل (Update)
  - حذف (Delete)
  - قراءة (Select)

- `domain/interfaces/`
  واجهات (Interfaces) لتعريف شكل الـ Repository لكل كيان.
  الهدف منها: تنظيم المشروع وفصل التنفيذ عن الاستخدام.

## 3) `DB/` — قاعدة البيانات

**الفائدة:** حفظ البيانات الفعلية للنظام.

- `DB/PostgreSQLDB.sql`
  نسخة كاملة من الجداول والبيانات (SQL Dump) للاستيراد في PostgreSQL.

### أهم الجداول

- `employees` بيانات الموظفين
- `users` حسابات المستخدمين وصلاحياتهم
- `allowances` علاوات الموظفين
- `deductions` خصومات الموظفين
- `bonuses` مكافآت الموظفين
- `salary_slips` كشوف الرواتب
- `reports` التقارير

## شرح سريع لتدفق العمل (Flow)

1) المستخدم يتعامل مع الواجهة في `frontend`.
2) الواجهة ترسل طلبًا من خلال `services/api`.
3) السيرفر يستقبل الطلب في `backend/routes`.
4) السيرفر ينفذ العملية من خلال `repositories`.
5) يتم تحديث البيانات في قاعدة البيانات `DB`.
6) السيرفر يرجع النتيجة للواجهة لعرضها.

## ملخص سريع للدكتورة (جملة جاهزة)

"المشروع مقسم لواجهة `frontend`، وسيرفر `backend`، وقاعدة بيانات `DB`.  
الواجهة فيها صفحات ومكونات وتصميم.  
السيرفر فيه مسارات الـ API ومنطق العمل والتعامل مع قاعدة البيانات عبر repositories.  
وقاعدة البيانات محفوظة في ملف SQL للاستيراد."

